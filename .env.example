# Django
DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=1
ALLOWED_HOSTS=localhost,127.0.0.1
# Use Postgres in Docker; for local dev without Docker comment this and fallback to sqlite
DATABASE_URL=postgres://phs:phs@localhost:5432/phs
# Optional Redis (OTP cache, Celery). If empty, falls back to locmem cache
REDIS_URL=redis://localhost:6379/1
# CORS origins (comma-separated)
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:3000
